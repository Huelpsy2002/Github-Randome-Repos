services:
  - type: web
    name: fullstack-app
    env: node
    plan: free
    buildCommand: |
      cd ../frontend && npm install && npm run build
    startCommand: npm start
    rootDir: backend

